before_install:
  - whoami
  - cat /etc/sudoers
  - su -c "apt-get update"
  - su -c "apt-get install -y libssl-dev autoconf automake libtool"

install:
  - ./autogen.sh
  - ./configure
  - make
  - make install
